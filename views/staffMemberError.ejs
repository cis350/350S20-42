<head>
    <script src="../mainLogin.js"></script>
    <link rel='stylesheet' href='/styles/stylesDashboard.css'/>
    <title>Vaccine App Dashboard</title>
</head>

<body class="container">
    <div class="topnav">
        <a class="active" href="/home">Home</a>
        <% if (user.hospitalOwner) {%>
            <a href="/myHospital">My Hospital</a>
        <% } %>
        <a href="/myRequests">My Requests</a>
        <% if (!user.medicalAccount) {%>
            <a href="/medicalRequest">Medical Upgrade</a>
        <% } %>
        <a href="/hospitalRequest">Hospital Creation</a>
        <% if (user.medicalAccount) {%>
            <a href="/addVaccineInfo">Add Vaccine Information</a>
        <% } %>
        <a href="/generalInformation">General Vaccine Information</a>
        <% if (user.medicalAccount) {%>
            <a href="/addScheduleSlots">Add Schedule Openings</a>
        <% } %>
        <a href="/viewAvailableSlots">Schedule Vaccine</a>
        <% if (user.medicalAccount) {%>
            <a href="/listScheduleRequest">Vaccine Request List</a>
        <% } %>
        <a href="/mySchedule">My Schedule</a>
        <a href="/logout">Logout</a>
    </div>

    <main>
        <h1 style='color: darkred;'>Error</h1>

        <% if (status == 0) {%>
           <p>Sorry, but the staff member you wanted to add/remove is not a medical account. It must be that an account is approved to be a medical account before they can be added/removed to a hosptial staff.</p>
        <%} else if (status == 1) { %>
            <p>Sorry, but the staff member you wanted to add/remove does not have an account. Make sure they make an account on the application, and are approved to be a medical account, and then try again!</p>
        <%} else if (status == 3) {%>
            <p>Sorry, but the staff member you wanted to remove does not exist in your hospital.</p>
        <% } else if (status == 2) {%>
            <p>Sorry, but the staff member you wanted to add already exists in your hospital!</p>
        <% } else if (status == -1) {%>
            <p>Please make sure that you fill out ALL parts of the form before submitting. Enter a hosptial and a username to add or remove.</p>
        <% } %>
    </main>
</body>
