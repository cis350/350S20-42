
<head>
    <script src="../mainLogin.js"></script>
    <link rel='stylesheet' href='/styles/stylesDashboard.css'/>
    <title>Vaccine App Dashboard</title>
</head>

<body class="container">
    <div class="topnav">
        <a class="active" href="/home">Home</a>
        <a href="/myHospital">My Hospital</a>
        <a href="/">Logout</a>
    </div>

    <main>
        <h1>Me!</h1>
        
        <% if (Array.isArray(user)) {%>
            <emph>Name: </emph> <%= user[0].username %>
            <br> 
            <emph>Is hospital owner: </emph> <% if (user[0].isHospitalOwner) {%>
            <p>Yes</p>
            <%} else {%>
            <p> no </p>
            <%} %>
        <%} else { %>
            <emph>Name: </emph> <%= user.username %>
            <br> 
            <emph>Is hospital owner: </emph> <% if (user.isHospitalOwner) {%>
            <p>Yes</p>
            <%} else {%>
            <p> no </p>
            <%} %>
        <%} %>
    </main>

</body>
